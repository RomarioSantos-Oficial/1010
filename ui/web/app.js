const messages=document.querySelector('#messages'),form=document.querySelector('#form'),input=document.querySelector('#input');
const userId=localStorage.lunaUserId||(localStorage.lunaUserId=`local_${crypto.randomUUID().slice(0,8)}`);
function bubble(text,kind){const el=document.createElement('article');el.className=kind;el.textContent=text;messages.append(el);messages.scrollTop=messages.scrollHeight;return el}
fetch('/health').then(r=>r.json()).then(data=>{document.querySelector('#status').textContent=data.model_ready?'Modelo local ativo':'Modo demonstração';document.querySelector('#provider').textContent=`Motor: ${data.llm_provider}`}).catch(()=>document.querySelector('#status').textContent='Servidor indisponível');
form.addEventListener('submit',async event=>{event.preventDefault();const text=input.value.trim();if(!text)return;bubble(text,'user');input.value='';const wait=bubble('Pensando…','assistant thinking');input.disabled=true;try{const response=await fetch('/chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({user_id:userId,text})});const data=await response.json();if(!response.ok)throw new Error(data.detail||'Erro');wait.textContent=data.spoken_text;wait.classList.remove('thinking')}catch(error){wait.textContent=`Não consegui responder: ${error.message}`;wait.classList.remove('thinking')}finally{input.disabled=false;input.focus()}});
input.addEventListener('keydown',event=>{if(event.key==='Enter'&&!event.shiftKey){event.preventDefault();form.requestSubmit()}});
document.querySelector('#forget').addEventListener('click',async()=>{if(!confirm('Apagar todo o histórico e as preferências desta identidade local?'))return;await fetch(`/memory/${userId}`,{method:'DELETE'});messages.replaceChildren();bubble('Memória apagada. Podemos recomeçar.','assistant')});

